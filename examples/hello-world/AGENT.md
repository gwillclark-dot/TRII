# AGENT.md — hello-world

> Smoke-test project for fresh TRII installs. Delete this directory once you have real projects.

## Project Overview
Trivial project that exists only to verify the full TRII loop end-to-end: orchestrator → agent → messaging adapter.

## Rules
- Don't write code. Don't commit. Just do the one task in NEXT_STEPS.md.
- Output a single line. The orchestrator will route it to the channel.
