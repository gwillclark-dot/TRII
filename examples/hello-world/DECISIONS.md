# Decisions

> Architectural memory. One row per decision. Keep it terse.

| Date | Decision | Why |
|------|----------|-----|
| 2026-04-21 | Ship `hello-world` as the default smoke-test project | Gives fresh installs a deterministic first-run signal that the full loop works before wiring real projects |
