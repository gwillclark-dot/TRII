# AGENT.md

TRII is a *harness* — it has no central agent. The agent is the per-project Claude Code session (or other agentic CLI) running in tmux.

Per-project agent identity lives in each project's `CLAUDE.md`. See [`templates/CLAUDE.md`](templates/CLAUDE.md) for the starting template.
