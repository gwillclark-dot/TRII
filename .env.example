# Slack bot token (xoxb-...) — from OAuth & Permissions page
SLACK_BOT_TOKEN=xoxb-your-token-here

# Slack app-level token (xapp-...) — for Socket Mode listener
SLACK_APP_TOKEN=xapp-your-app-token-here

# Override TRII_HOME if not running from the repo directory
# TRII_HOME=/path/to/TRII
